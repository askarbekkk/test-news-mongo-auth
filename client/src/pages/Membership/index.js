import React from 'react';
import Layout from "../../components/Layout";

const Membership = () => {
    return (
        <Layout>
            Membership
        </Layout>
    );
};

export default Membership;